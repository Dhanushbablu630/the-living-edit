import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { cookieName, validAdminSession } from "@/lib/admin-session";
import { createSupabaseAdminClient } from "@/lib/supabase-server";
import styles from "../admin.module.css";
export default async function TestimonialsAdmin() { const store = await cookies(); if (!validAdminSession(store.get(cookieName)?.value)) redirect("/admin/login"); const { data } = await createSupabaseAdminClient().from("testimonials").select("id,client_name,quote,role_or_project,is_active").order("created_at", { ascending: false }); const testimonials = data ?? []; return <main className={styles.dashboard}><aside><a href="/" className={styles.logo}>THE LIVING <span>EDIT</span></a><nav><a href="/admin">Overview</a><a href="/admin/projects">Projects</a><a className={styles.active} href="/admin/testimonials">Testimonials</a></nav></aside><section className="adminEditor"><header><div><p>Content management</p><h1>Client words.</h1></div><a href="/admin">Back to dashboard</a></header><form className="adminForm" action="/api/admin/testimonials" method="post"><h2>Add a testimonial</h2><input name="client_name" required placeholder="Client name" /><input name="role_or_project" placeholder="Project or location" /><input name="image_url" placeholder="Portrait image URL (optional)" /><textarea name="quote" required placeholder="Client quote" /><label><input name="is_active" type="checkbox" defaultChecked /> Publish this testimonial</label><button>Add testimonial</button></form><div className={styles.table}><div className={styles.tableHead}><div><p>Testimonials</p><span>Only published testimonials appear on the website</span></div></div>{testimonials.map((testimonial) => <article className="adminProject" key={testimonial.id}><div><strong>{testimonial.client_name}</strong><span>{testimonial.role_or_project ?? "Client"} · {testimonial.is_active ? "Published" : "Hidden"}</span><p>{testimonial.quote}</p></div><form action="/api/admin/testimonials" method="post"><input type="hidden" name="intent" value="delete" /><input type="hidden" name="id" value={testimonial.id} /><button className="adminDelete">Delete</button></form></article>)}{!testimonials.length && <div className={styles.empty}>No testimonials yet.</div>}</div></section></main>; }
