import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { cookieName, validAdminSession } from "@/lib/admin-session";
import { createSupabaseAdminClient } from "@/lib/supabase-server";
import styles from "../admin.module.css";
export default async function ProjectsAdmin() { const store = await cookies(); if (!validAdminSession(store.get(cookieName)?.value)) redirect("/admin/login"); const { data } = await createSupabaseAdminClient().from("projects").select("id,name,category,city,status,thumbnail_url,created_at").order("created_at", { ascending: false }); const projects = data ?? []; return <main className={styles.dashboard}><aside><a href="/" className={styles.logo}>THE LIVING <span>EDIT</span></a><nav><a href="/admin">Overview</a><a className={styles.active} href="/admin/projects">Projects</a><a href="/admin/testimonials">Testimonials</a></nav></aside><section className="adminEditor"><header><div><p>Content management</p><h1>Projects & gallery.</h1></div><a href="/admin">Back to dashboard</a></header><form className="adminForm" action="/api/admin/projects" method="post"><h2>Add a project</h2><input name="name" required placeholder="Project name" /><input name="category" placeholder="Category (e.g. 3 BHK)" /><input name="city" placeholder="City" /><input name="thumbnail_url" placeholder="Cover image URL" /><textarea name="description" placeholder="Short project description" /><select name="status"><option value="draft">Draft</option><option value="published">Published</option></select><label><input name="featured" type="checkbox" /> Feature on homepage</label><button>Add project</button></form><div className={styles.table}><div className={styles.tableHead}><div><p>Projects</p><span>Add gallery image URLs below each project</span></div></div>{projects.map((project) => <article className="adminProject" key={project.id}><div><strong>{project.name}</strong><span>{project.category} · {project.city ?? "No city"} · {project.status}</span></div><form action="/api/admin/projects" method="post"><input type="hidden" name="intent" value="image" /><input type="hidden" name="project_id" value={project.id} /><input name="url" required placeholder="Image URL" /><input name="alt_text" placeholder="Image description" /><select name="image_type"><option value="gallery">Gallery</option><option value="before">Before</option><option value="after">After</option><option value="render_3d">3D render</option></select><button>Add image</button></form><form action="/api/admin/projects" method="post"><input type="hidden" name="intent" value="delete" /><input type="hidden" name="id" value={project.id} /><button className="adminDelete">Delete</button></form></article>)}{!projects.length && <div className={styles.empty}>No projects yet. Add your first studio project above.</div>}</div></section></main>; }
